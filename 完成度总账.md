# 青山·完成度总账 v2（基于四份独立审查）

> v2：2026-09-05 ｜ 取代凭记忆写的 v1
> 证据源：四份独立审查报告（`审查/01~04`，合计 140+ 条判定，每条带双向源码行号；3 个 P0 已由主理人抽查复核实锤）
> 规矩不变：每轮交付前对照本账；"没做/简化"必须有行；**本账与四份报告冲突时以报告为准**。

---

## 一、审查判定总统计

| 报告 | 判定数 | ✅ | ⚠️ | ❌ |
|---|---|---|---|---|
| 01-UI 对照 HTML | 57 | 26 | 22 | 9 |
| 02-Agent 语义对照 dsh | 34 | 12 | 14 | 8 |
| 03-记忆对照 Codex | 32 | 11 | 12 | 9 |
| 04-架构/14 模块 | 14 模块 | 1 | 8 | 5（M7 计划内） |
| **合计** | **140+** | **50** | **56** | **31** + 22 项总账漏项 |

M0-M8 验收核对：M0 ✅ / M2 ✅ / M5 核心语义 ✅ / M1·M3·M4·M6 ⚠️（核心通但有缺口）/ M7·M8 ❌。

---

## 二、P0 缺陷清单（安全/数据，已验证，修复前不进 M7）

### 安全
- **S1-1 审批免审绕过**：`Approval.swift` needsApproval 的 isSafe 第二个 `contains(where:)` **无 anchored**——`cat x; rm -rf /` 命中 `^cat\b` 免审放行。修：删除无锚点检查，全部走 anchored 整条匹配。
- **S1-2 write_file heredoc 逃逸**：内容含单独一行 `QSEOF` 即截断/注入任意命令。修：改 base64 管道（`echo <b64> | base64 -d > path`）。
- **S1-3 脱敏缺失**：Phase1 transcript 原样上传，密钥可落盘 entries.json。修：上传前+入库前跑确定性 redact（token/password 正则集）。

### 数据完整性
- **D-1 resume 写坏日志**：`load()` 重建 SessionLog 不恢复 seq 游标 → 继续聊重写 header/seq 从 1 重排。修：SessionLog 增加 `resume(seq:)` 或 load 时读末行 seq。
- **D-2 未知工具回喂格式**：只回 llmHistory，落盘 agent/message → resume 后 tool_calls 无结果，DeepSeek 400。修：未知工具也落 tool/call+tool/result。
- **D-3 重命名破坏 append-only**：全量重写 JSONL 且与 append 队列竞态。修：标题存 header 外部（meta 文件），不改日志。
- **D-4 entries.json 损坏静默清库**：load 失败置空 → 下次整合清空全部。修：解析失败时备份原文件并拒载。
- **D-5 compact 重建丢注入**：compact 后历史用裸 systemPrompt，丢 AGENTS.md/记忆摘要。修：compactedHistory 用 initialSystemPrompt。

### 正确性
- **C-1 引用排序比较器错位**：`($0.usageCount, $1.lastUsage??$1.generatedAt) > ($1.usageCount, $0.lastUsage??$0.generatedAt)` tiebreak 反向 → 新记忆被 top-N 截掉。一行修：`($0.usageCount, $0.lastUsage ?? $0.generatedAt)`。
- **C-2 上下文窗口 8k 硬编码**（CtxUsage）：真实窗口 64k-128k → 过早 compact + CtxRing 虚高 8-16 倍。修：常量改 64_000 并可配。
- **C-3 mem.extracted 永久标记**：会话只提取一次，长会话后半段永远丢失。修：记录已提取的事件水位（seq），超水位增量再提取。
- **C-4 右栏空态"浏览器"错开终端**：RootView rightRow("safari"...,"term")。一行修。

### 能力缺口（高优先）
- **G-1 turn 无取消/审批无超时**：失控 turn 只能等 12 步护栏；审批无应答永久挂起（违 fail-closed）。修：LLM 流 Task 取消 + 审批卡 120s 超时默认 deny。
- **G-2 输出无截断上限**：大 cat 全量进 llmHistory。修：16k 字符 + `<clipped>` 提示（dsh 语义）。
- **G-3 governor 未接线**：ISHKernel.m:1581-1830 **已完整实现** CPU governor，Swift 侧零调用。修：boot 后调用 + beginBackgroundTask 保护管线。
- **G-4 shell 退出无自愈**：exit 后每条命令静默吃满 30s。修：检测 shell 死亡 → 重建 + 提示。
- **G-5 项目隔离**（记忆+执行目录重置）——待确认口径后做。

---

## 三、逐模块状态（修订版）

| 模块 | 状态 | 主要缺口 |
|---|---|---|
| LLMAdapter | ✅ | 重试无 jitter；非流式接口缺（管线复用 stream 拼装） |
| AgentCore（循环） | ⚠️ | 无 inbox/中断/并行调度；未知工具落盘格式 |
| SessionLog | ⚠️ | resume 游标、重命名破坏 append-only、无 fsync |
| ContextAssembler | ⚠️ | 窗口硬编码、compact 丢注入、输出无截断 |
| ToolKit | ⚠️ | heredoc 逃逸、工具少（web/编辑器类 M7） |
| Approval | ⚠️ | 白名单绕过、allowAlways 粒度过粗（整个工具）、审批无超时 |
| ISHBridge | ⚠️ | governor 未接线、shell 无自愈、无 reset |
| Memory | ⚠️ | 整合无去重/溯源、单向覆盖、脱敏缺失、提取水位、**项目隔离** |
| AppShell | ⚠️ | 键盘唤起、快捷键、向导、ANSI 清理 |
| SkillKit/Subagent/WebTools/MCP/TodoJobs | ❌ | M7 本体 |
| UI 对 HTML | ⚠️ | 消息类型×3、diff 渲染、审查完整版、浏览器、设置补全 |
| 工程化 | ❌ | 零测试、版本号三处不一致、rootfs 无 sha256、崩溃恢复演练 |

---

## 四、修复批次路线（建议，待拍板）

**批 1 · 止血（P0，约半天）**：S1-1/S1-2/S1-3 + D-1/D-2/D-3/D-4/D-5 + C-1/C-2/C-4 —— 全部是小改（1-10 行级），但都是安全/数据级。
**批 2 · 能力补全（约一天）**：G-1 取消+审批超时 / G-2 截断 / G-3 governor 接线 / G-4 shell 自愈 / G-5 项目隔离 / C-3 提取水位。
**批 3 · UI 对齐（约一天）**：消息类型×3 + diff 渲染 + 审查 tab 完整版 + 浏览器空态修复 + 设置补全 + ANSI 清理。
**批 4 · M7 本体**：任务队列/插件 MCP/技能/设置完善（占位入口的真身）。
**批 5 · M8**：测试补齐/崩溃恢复演练/RootfsPatch/发布工程。

## 五、完成度量化（v2，以审查为据）

| 维度 | v1 凭记忆 | v2 审查后 | M7+M8 后 |
|---|---|---|---|
| 主流程能力 | ~80% | **~75%**（安全/数据洞拉低） | ~92% |
| UI 对齐 HTML | ~75% | **~68%**（57 条里 31 条非✅） | 批3后 ~90% |
| Agent 语义对齐 dsh | ~70% | **~65%**（inbox/中断/并行三大架构缺失） | M8 后 ~80% |
| 记忆对齐 Codex | ~85%（自评过高） | **~65%**（整合/脱敏/水位缺口） | 批2后 ~85% |
| 产品健壮性 | ~25% | **~20%**（22 项漏项大头在此） | M8 后 ~85% |
| **综合（自用 Beta）** | ~70% | **~65%** | **~90%** |
| **综合（对外产品）** | ~50% | **~45%** | M8 后 ~70% |

## 六、总账漏项补录（v1 漏掉的 22 项，详见报告 04 第五节）

审批白名单绕过 / heredoc 逃逸 / turn 无取消 / compact 丢注入 / 输出无截断 / 8k 窗口失真 / 重命名破坏日志 / 退后台管线无保护 / rootfs 无校验 / 版本号不一致 / governor 已有未接线 / 引用排序 bug / entries.json 损坏清库 / 提取永久水位 / 脱敏缺失 / 键盘唤起 / 分屏快捷键 / 深色模式 / fsync 缺失 / allowAlways 粒度 / 多会话并行 / JSONL 竞态。
