name: QingShan

options:
  bundleIdPrefix: com.qingshan
  deploymentTarget:
    iOS: "16.0"
  createIntermediateGroups: true

settings:
  base:
    SWIFT_VERSION: "5.9"
    TARGETED_DEVICE_FAMILY: "2"          # iPad only
    CODE_SIGN_STYLE: Automatic
    DEVELOPMENT_TEAM: ""

targets:
  QingShan:
    type: application
    platform: iOS
    sources:
      - path: QingShan
      # 宿主桥（ObjC，链接 iSH 静态库）
      - path: Platform
        # 排除 rootfs.tar（由下方单文件资源引入，避免被当源码扫描）
        excludes:
          - "Resources/rootfs.tar"
      # Alpine rootfs（fakefs tar：data/ + meta.db），单文件资源
      - path: QingShan/Resources/rootfs.tar
        buildPhase: resources
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.qingshan.juyang
        MARKETING_VERSION: "0.0.1"
        CURRENT_PROJECT_VERSION: "1"
        GENERATE_INFOPLIST_FILE: NO
        SWIFT_OBJC_BRIDGING_HEADER: QingShan/Bridge/QingShan-Bridging-Header.h
        # 双根：Vendor/include/ish（构建生成头）+ Vendor/ish（源码内部互 include 的根）
        HEADER_SEARCH_PATHS: "$(SRCROOT)/Vendor/include/ish $(SRCROOT)/Vendor/ish"
        LIBRARY_SEARCH_PATHS: "$(SRCROOT)/Vendor/libs"
        OTHER_LDFLAGS: "-lish -lish_emu -lfakefs"
        CLANG_ENABLE_MODULES: YES
        # iSH 头文件按 GUEST_ARM64 条件编译（对齐 Vendor/ish/app/GuestARM64.xcconfig）
        GCC_PREPROCESSOR_DEFINITIONS: "$(inherited) GUEST_ARM64=1"
        # iSH 头里的 C 声明以 ObjC(.m) 编译；QCExecutor.m 需 ObjC++? 实为 .m 即可（纯 C API）
    dependencies:
      - sdk: libsqlite3.tbd
      - sdk: libz.tbd
      - sdk: libbz2.tbd
      - sdk: libresolv.tbd
    info:
      path: QingShan/Info.plist
      properties:
        CFBundleDisplayName: 青山
        CFBundleShortVersionString: "0.0.1"
        CFBundleVersion: "2"
        UILaunchScreen: {}
        UISupportedInterfaceOrientations:
          - UIInterfaceOrientationLandscapeLeft
          - UIInterfaceOrientationLandscapeRight
        ITSAppUsesNonExemptEncryption: false
